/**
 * Cities that appear on the public (production) map.
 * In dev mode all cities are visible regardless of this list.
 *
 * To publish a new city: add its name here (must match the `city` field
 * in the place frontmatter exactly).
 */
export const PUBLISHED_CITIES: string[] = [
  "Bangkok",
];
